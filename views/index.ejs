<!DOCTYPE html>
<html lang="en">
<head>
<%- include("./partials/head.ejs")%>
<link rel="stylesheet" href="/css/style.css">
</head>
<%- include("./partials/header.ejs")%>
<body>
    <div id="allData">
<% if (game.length > 0) {%>
<% console.log("Game Acquiered") %>
    <% game.forEach(data => { %>
        <a href="/home/game/<%= data.id %>">
        <div class="dataBase">
            <img src="<%= data.link %>" alt="<%= data.name %>">
            <h2><%= data.name %></h2>
        </div>
        </a>
    <% }) %>
    <% } %>

        <%if (anime.length > 0){  %>
            <% anime.forEach(data =>{ %>
                <a href="/home/anime/<%= data.id %>">
                <div class="dataBase">
               <img src="<%= data.link %>" alt="">
               <h2><%= data.Name %></h2>
               </div>
               </a>
             <% })%>
            <% } %>
    </div>
</body>
</html>